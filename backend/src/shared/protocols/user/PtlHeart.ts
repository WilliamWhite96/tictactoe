import { BaseRequest, BaseResponse, BaseConf } from "../base";

export interface ReqHeart extends BaseRequest {
    
}

export interface ResHeart extends BaseResponse {
    now:number
}

export const conf: BaseConf = {
    
}